function setup() {
  createCanvas(400, 400);
  var box = createInput();
  box.position(5,60);
  var button = createButton("BTS");
  button.position(200,200);
  button.mousePressed();
}

function draw() {
  background(220); 
}